# サプライチェーン対策
# 公開直後の悪性バージョンを掴まないよう、公開から 7 日未満のバージョンは
# 依存解決の対象から除外する (cooldown)。npm 11 以降の min-release-age を利用。
min-release-age=7

# 依存パッケージの install ライフサイクルスクリプト (postinstall 等) を実行しない。
# 本リポジトリは puppeteer-core のみでネイティブビルドやブラウザの自動 DL が
# 無いため、無効化してもインストール・ビルドに影響しない。
ignore-scripts=true
